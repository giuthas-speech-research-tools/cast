name: siwortkit-stable
channels:
  - conda-forge 
  - defaults
dependencies:
  - python >= 3.10
  - numpy >= 1.22
  - scipy >= 1.7
  - matplotlib >= 3.5
  - pylint >= 2.9
  - pip 
  # textgrids is instead installed in devel mode with 
  # pip -e .
  # in the source directory (~/tyot/source/textgrids for me)
  # - pip:
  #   - praat-textgrids >= 1.3

